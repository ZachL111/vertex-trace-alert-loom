# Golden Cases

The golden set keeps `span volume` and `signal loss` visible as named cases.

The main golden fixture is `fixtures/golden/scoreboard.csv`. The matrix fixture is `fixtures/golden/lane-matrix.csv`. Together they cover `span volume`, `latency skew`, `signal loss`, and `incident shape` with different score ranges.

The purpose is to make intentional rule changes show up in review.
